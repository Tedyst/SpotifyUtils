export interface SongCardProps {
    duration?: number,
    name: string,
    artist: string,
    image?: string,
    count?: number,
    text?: string,
    shadow?: boolean,
    uri?: string,
    usingLink?: boolean,
}
